# 2026-06-08

## 한 것
- Next.js 16에서 `params`가 Promise로 바뀐 것 확인 → `use(params)`로 수정
- Anthropic API 결제 문제로 Gemini API로 전환
- `@google/generative-ai` SDK 설치 및 Route Handler 연동
- Gemini 스트리밍 응답 구현 (`sendMessageStream`)
- 면접관 페르소나 4개 추가 (스타트업 CTO, 대기업 HR, 외국계 HR, 스타트업 CEO)
- Tailwind CSS로 메인 페이지 + 채팅 페이지 UI 스타일링
- GitHub 레포 생성 및 첫 푸시

## 배운 것
- `params`는 Next.js 15부터 Promise → 클라이언트 컴포넌트에서는 `React.use()`로 unwrap
- Gemini 메시지 형식은 Claude와 다름: `role`이 `assistant` 대신 `model`, `content` 대신 `parts: [{text}]`
- 문자열 안에서 줄바꿈하면 TypeScript 오류남 → 한 줄로 쓰거나 백틱(`\``) 사용

## 느낀 것
- 개념에 대해서 확실하게 공부할것
- 페이지간 흐름 느끼기

## 다음에 할 것
- 전송 중 비활성화 (`isLoading` 상태)
- 새 메시지 오면 자동 스크롤
- 면접 시작 시 AI가 먼저 첫 질문 자동 시작
- 면접 종료 후 피드백 기능
